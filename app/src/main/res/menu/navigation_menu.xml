<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/menu_item_main"
            android:title="@string/menu_item_main_title"
            android:icon="@drawable/ic_feed"/>
        <item
            android:id="@+id/menu_item_categories"
            android:title="@string/menu_item_categories_title"
            android:icon="@drawable/ic_categories"/>
        <item
            android:id="@+id/menu_item_subscriptions"
            android:title="@string/menu_item_subscriptions_title"
            android:icon="@drawable/ic_bookmark"/>
    </group>
    <group android:id="@+id/addition_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/menu_item_settings"
            android:title="@string/action_settings"
            android:icon="@drawable/ic_settings"/>
    </group>
</menu>